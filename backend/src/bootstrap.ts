export default async () => {
  console.log('🚀 Bootstrap started - nothing to seed as we use enums for game systems and factions');
  
  // This bootstrap is simplified since we use enums instead of relations for game systems and factions
  // If you need to seed any initial data in the future, add it here
  
  console.log('✅ Bootstrap completed successfully!');
};